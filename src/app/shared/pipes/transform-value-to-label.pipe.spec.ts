import { TransformValueToLabelPipe } from './transform-value-to-label.pipe';

describe('TransformValueToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformValueToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
