import { SumFoodToMakePipe } from './sum-food-to-make.pipe';

describe('SumFoodToMakePipe', () => {
  it('create an instance', () => {
    const pipe = new SumFoodToMakePipe();
    expect(pipe).toBeTruthy();
  });
});
