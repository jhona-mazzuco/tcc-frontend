import { TransformValueToTimePipe } from './transform-value-to-time.pipe';

describe('TransformValueToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformValueToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
